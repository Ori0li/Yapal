import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "i.scdn.co",
      "blog.kakaocdn.net",
      "helpx.adobe.com",
      "qi-b.qoo10cdn.com",
    ],
  },
};

export default nextConfig;
